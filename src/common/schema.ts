import { z } from "zod";
export const zoiSchema = z.object({
  //   name: z.string().min(2, { message: "min string length 2 " }),
  //   description: z.string().min(10, { message: "min string length 5 " }),
  //   code: z.number().min(2, { message: "min string length 2 " }),
  //   email: z.string().email(),
});
